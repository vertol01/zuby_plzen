import * as b from 'bobril';

export let bodyStyle = b.styleDef({
    height: '100%',
    margin: 0
})

export let htmlStyle = b.styleDef({
    height: '100%',
    margin: 0,
    marginLeft: 'calc(100vw - 100%)'
})

export let mainContentContainerStyle = b.styleDef({
    height: '100%',
    margin: '0 auto'
})